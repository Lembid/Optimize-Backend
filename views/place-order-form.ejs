<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Place Order Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }
        h1, h2, h3 {
            color: #333;
        }
        .section {
            background-color: #fff;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        #cardsContainer {
            display: flex;
            flex-direction: row;
            justify-content: space-between;
        }
        .card {
            border: 1px solid #ddd;
            padding: 15px;
            margin-right: 15px;
            border-radius: 5px;
            background-color: #f9f9f9;
            flex: 1;
        }
        .card:last-child {
            margin-right: 0;
        }
        input[type="text"], input[type="number"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin-right: 10px;
        }
        button:hover {
            background-color: #45a049;
        }
        input[type="submit"] {
            background-color: #008CBA;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }
        input[type="submit"]:hover {
            background-color: #007B9A;
        }
    </style>
</head>
<body>
    <%- include('partials/admin-navbar') %>
    <h1>Edit Place Order Page</h1>
    <form action="/admin/place-order" method="POST">
        <div class="section">
            <label for="mainTitlePart1">Main Title Part 1:</label>
            <input type="text" id="mainTitlePart1" name="mainTitlePart1" value="<%= pageData.mainTitlePart1 %>"><br><br>

            <label for="mainTitlePart2">Main Title Part 2:</label>
            <input type="text" id="mainTitlePart2" name="mainTitlePart2" value="<%= pageData.mainTitlePart2 %>"><br><br>

            <label for="subHeading">Sub Heading:</label>
            <input type="text" id="subHeading" name="subHeading" value="<%= pageData.subHeading %>"><br><br>
        </div>

        <div class="section">
            <h2>Cards</h2>
            <div id="cardsContainer">
                <% for(let i = 0; i < 3; i++) { %>
                    <div class="card">
                        <input type="text" name="cards[<%= i %>][title]" value="<%= pageData.cards[i] ? pageData.cards[i].title : '' %>" placeholder="Card Title">
                        <input type="text" name="cards[<%= i %>][description]" value="<%= pageData.cards[i] ? pageData.cards[i].description : '' %>" placeholder="Card Description">
                    </div>
                <% } %>
            </div>
        </div>

        <input type="submit" value="Save Changes">
    </form>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $('form').on('submit', function(e) {
            e.preventDefault();
            $.ajax({
                url: '/admin/place-order',
                method: 'POST',
                data: $(this).serialize(),
                success: function(response) {
                    alert('Bhai, changes save ho gaye hain!');
                },
                error: function(error) {
                    alert('Oye, changes save karne mein kuch gadbad ho gayi. Phir se try karo.');
                }
            });
        });
    </script>
</body>
</html>
